{% extends 'base.html.twig' %}

{% block body %}
    <h1>Post creation</h1>
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
    <ul>
        <li>
            <a href="{{ path('post_index') }}">Pokaż wszystkie ogłoszenia</a>
        </li>
    </ul>
{% endblock %}

